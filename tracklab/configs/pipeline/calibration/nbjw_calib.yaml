_target_: tracklab.pipeline.calibration.NBJW_Calib
image_width: 1920
image_height: 1080
batch_size: 32  # Lower batch size for more stable updates
use_prev_homography: True  # Temporal consistency between frames
ransac_iter: 1000  # Very high for unusual viewpoints (robust outlier rejection)
normalize_keypoints: True  # Ensure keypoints are properly normalized