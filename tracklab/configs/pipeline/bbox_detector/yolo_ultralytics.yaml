_target_: tracklab.pipeline.bbox_detector.yolo_ultralytics_api.YOLOUltralytics
batch_size: 8
cfg:
  # models available :
  # yolo11n.pt yolo11s.pt yolo11m.pt yolo11l.pt yolo11x.pt
  # those models will be downloaded automatically if not found in the path
  path_to_checkpoint: "${model_dir}/yolo/yolo11m.pt"

  # Raise confidence to curb the flood of false positives while keeping distant players
  min_confidence: 0.55
  postproc:
    # filter out tiny boxes ( <0.03% of frame area ~ 30x30 px on 1080p )
    size_filter_min_area_ratio: 3e-4
