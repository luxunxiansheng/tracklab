{
	"name": "TrackLab Container (Python 3.9 + UV)",
	"build": {
		"context": "..",
		"dockerfile": "Dockerfile"
	},
	"runArgs": [
		"--gpus=all",
		"--shm-size=10g",
		"--network=host"
	],
	"containerEnv": {
	  "http_proxy": "${localEnv:http_proxy}",
      "https_proxy": "${localEnv:https_proxy}",
      "no_proxy": "${localEnv:no_proxy}"
	},
	"mounts": [
		"source=${localEnv:HOME}/data,target=/workspaces/${localWorkspaceFolderBasename}/data,type=bind,consistency=cached"
	],
	"postCreateCommand": "bash -c 'uv sync && echo \"source .venv/bin/activate\" >> ~/.bashrc && chmod +x .devcontainer/post-install.sh && .devcontainer/post-install.sh'",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"ms-python.vscode-pylance",
				"ms-python.debugpy",
				"ms-python.black-formatter",
				"ms-python.vscode-python-envs",
				"ms-toolsai.jupyter",
				"ms-toolsai.datawrangler",
				"shd101wyy.markdown-preview-enhanced",
				"yzhang.markdown-all-in-one",
				"bierner.markdown-checkbox",
				"bierner.markdown-mermaid",
				"DavidAnson.vscode-markdownlint",
				"marp-team.marp-vscode",
				"mhutchie.git-graph",
				"RSIP-Vision.nvidia-smi-plus",
				
			],
			"settings": {
				"python.defaultInterpreterPath": "/workspaces/tracklab/.venv/bin/python",
				"python.terminal.activateEnvironment": true,
				"python.analysis.autoImportCompletions": true,
				"python.analysis.typeCheckingMode": "basic"
			}
		}
	}
}
